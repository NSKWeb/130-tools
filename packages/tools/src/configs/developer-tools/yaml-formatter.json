{
  "id": "yaml-formatter",
  "slug": "yaml-formatter",
  "name": "YAML Formatter",
  "description": "Format and validate YAML configuration files.",
  "shortDesc": "Format YAML files",
  "category": "developer-tools",
  "icon": "⚙️",
  "inputs": [
    {
      "id": "yaml",
      "type": "textarea",
      "label": "Enter YAML",
      "placeholder": "Paste your YAML here...",
      "required": true,
      "rows": 15
    },
    {
      "id": "indentSize",
      "type": "number",
      "label": "Indent Size",
      "placeholder": "Spaces per indent",
      "required": true,
      "defaultValue": 2,
      "validation": {
        "min": 1,
        "max": 8
      }
    }
  ],
  "outputs": [
    {
      "id": "result",
      "type": "textarea",
      "label": "Formatted YAML"
    },
    {
      "id": "valid",
      "type": "text",
      "label": "Valid YAML"
    }
  ],
  "logic": {
    "type": "client",
    "function": "yaml-formatter"
  },
  "seo": {
    "title": "YAML Formatter - Format and Validate YAML",
    "description": "Format and validate YAML configuration files with proper indentation.",
    "keywords": ["yaml formatter", "format yaml", "yaml validator", "yaml beautifier"]
  }
}
