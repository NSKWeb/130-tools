{
  "id": "uuid-generator",
  "slug": "uuid-generator",
  "name": "UUID/GUID Generator",
  "description": "Generate unique UUIDs and GUIDs for your applications. Supports v4 UUIDs and different formats.",
  "shortDesc": "Generate unique UUIDs",
  "category": "generators",
  "icon": "🆔",
  "inputs": [
    {
      "id": "count",
      "type": "number",
      "label": "Number of UUIDs",
      "placeholder": "How many UUIDs?",
      "required": true,
      "defaultValue": 1,
      "validation": {
        "min": 1,
        "max": 100
      }
    },
    {
      "id": "uppercase",
      "type": "checkbox",
      "label": "Uppercase",
      "defaultValue": false
    },
    {
      "id": "withBraces",
      "type": "checkbox",
      "label": "Include braces",
      "defaultValue": true
    },
    {
      "id": "withHyphens",
      "type": "checkbox",
      "label": "Include hyphens",
      "defaultValue": true
    }
  ],
  "outputs": [
    {
      "id": "uuids",
      "type": "text",
      "label": "Generated UUIDs"
    }
  ],
  "logic": {
    "type": "client",
    "function": "uuid-generator"
  },
  "seo": {
    "title": "UUID Generator - Free UUID/GUID Generator",
    "description": "Generate unique UUIDs and GUIDs for your applications. v4 UUIDs with custom formatting options.",
    "keywords": ["uuid generator", "guid generator", "unique id", "v4 uuid"]
  }
}
