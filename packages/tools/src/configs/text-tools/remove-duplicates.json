{
  "id": "remove-duplicates",
  "slug": "remove-duplicate-lines",
  "name": "Remove Duplicate Lines",
  "description": "Remove duplicate lines from text. Great for cleaning lists, removing repeated entries, and data deduplication.",
  "shortDesc": "Remove duplicate lines from text",
  "category": "text-tools",
  "icon": "🧹",
  "inputs": [
    {
      "id": "text",
      "type": "textarea",
      "label": "Enter text (one item per line)",
      "placeholder": "Paste your list here...",
      "required": true
    },
    {
      "id": "caseSensitive",
      "type": "checkbox",
      "label": "Case sensitive",
      "defaultValue": false
    },
    {
      "id": "trimSpaces",
      "type": "checkbox",
      "label": "Trim whitespace",
      "defaultValue": true
    }
  ],
  "outputs": [
    {
      "id": "result",
      "type": "textarea",
      "label": "Result"
    },
    {
      "id": "removed",
      "type": "number",
      "label": "Duplicates Removed"
    }
  ],
  "logic": {
    "type": "client",
    "function": "remove-duplicates"
  },
  "seo": {
    "title": "Remove Duplicate Lines - Free Text Deduplication Tool",
    "description": "Remove duplicate lines from text lists. Great for cleaning data and lists.",
    "keywords": ["remove duplicate lines", "deduplicate text", "remove duplicates", "clean list"]
  }
}
